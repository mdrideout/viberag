node_modules
dist
# Viberag index
.viberag/
# Embedding model cache
local_cache/
# MCP config (local, not committed)
.mcp.json
# MCP config (local, not committed)
.roo
# MCP config (local, not committed)
.gemini/settings.json
# MCP config (local, not committed)
.roo/mcp.json
# MCP config (local, not committed)
.vscode/mcp.json
