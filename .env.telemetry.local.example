# Local telemetry key baking (gitignored: copy to .env.telemetry.local)
#
# Usage:
#   cp .env.telemetry.local.example .env.telemetry.local
#   npm run build:telemetry
#
# These values are used ONLY to bake keys into dist/ for local testing.
# Runtime does not read keys from env vars.

# PostHog Project API key (write-only ingest key), usually starts with phc_
VIBERAG_BAKE_POSTHOG_KEY=phc_your_posthog_project_api_key_here

# Sentry DSN (project settings)
VIBERAG_BAKE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

